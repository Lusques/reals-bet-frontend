import { SliceOrAllPipe } from './slice-or-all.pipe';

describe('SliceOrAllPipe', () => {
  it('create an instance', () => {
    const pipe = new SliceOrAllPipe();
    expect(pipe).toBeTruthy();
  });
});
